[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand   = "sh apps/hub-api/start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile && pnpm --filter @icproject/hub-api exec npx prisma generate --schema=prisma/schema.prisma",
  "builds": [
    {
      "src": "apps/hub-api/api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "apps/hub-api/prisma/**"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/apps/hub-api/api/index.ts" }
  ]
}
